import { listInsights, listDailyLogs, saveInsight } from './data.js';
import { callAI } from './api.js';
import { todayISO, escapeHTML, formatAISummary, toast } from './utils.js';
export function dashboardPage(state){const insights=state.insights||[];const html=insights.length?insights.map(i=>`<div class="p-4 rounded-lg border bg-gray-50"><p class="font-semibold text-gray-900">${escapeHTML(i.type)}</p><p class="text-sm mt-1">${formatAISummary(i.text)}</p></div>`).join(''):'<p class="text-gray-400">No insights yet.</p>';return `<div><div class="flex justify-between items-center mb-2"><div><h1 class="text-4xl font-bold">Hello, ${escapeHTML(state.profile?.full_name||'there')}</h1><p class="text-lg text-gray-500 mt-2">Here is your health overview for today.</p></div></div><div class="dashboard-card p-8 my-8 border-l-4 border-l-accent"><h2 class="text-3xl font-bold mb-3">Your AI Health Command Center</h2><p class="text-gray-600 text-lg">VitalSync securely stores your check-ins and clinical reports in your Supabase account and uses AI to summarize patterns. AI output is informational and is not a diagnosis.</p></div><h2 class="text-3xl font-bold mb-6">Today's Focus</h2><div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mb-10"><div class="dashboard-card p-6 flex flex-col justify-between"><div><h3 class="text-2xl font-semibold mb-2">Clinical Reports</h3><p class="text-gray-600 mb-4">Upload a PDF or image securely and request an AI-assisted summary.</p></div><button data-nav="upload-report" class="btn-primary font-semibold py-2 px-4 rounded-lg">Upload Report</button></div><div class="dashboard-card p-6 flex flex-col justify-between"><div><h3 class="text-2xl font-semibold mb-2">Daily Check-in</h3><p class="text-gray-600 mb-4">Complete a short guided check-in and keep your health log organized.</p></div><button data-nav="chat" class="btn-primary font-semibold py-3 rounded-lg">Start Conversation</button></div><div class="dashboard-card p-6 flex flex-col"><h3 class="text-2xl font-semibold mb-3">Recent Insights</h3><div class="flex-1 space-y-3 overflow-auto" style="max-height:200px">${html}</div><button data-nav="history" class="mt-4 w-full bg-gray-100 text-gray-800 font-semibold py-2 rounded-lg">View History</button></div></div><h2 class="text-3xl font-bold mb-6">Recent Insights</h2><div class="dashboard-card p-5 space-y-4">${html}</div></div>`}
export async function loadDashboardState(state){try{state.logs=await listDailyLogs();state.insights=await listInsights();}catch(e){toast(e.message,'error')}return state}
export async function createInsightFromLogs(state){const logs=state.logs||[];if(logs.length<3)return;const text=await callAI({action:'chat',prompt:`Analyze these recent VitalSync check-in logs and give one short, general wellness insight. Do not diagnose or prescribe. Data: ${JSON.stringify(logs.slice(0,7))}`});const insight=await saveInsight('Weekly Insight',text);state.insights=[insight,...(state.insights||[])];return insight}
